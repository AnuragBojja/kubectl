apiVersion: v1 
kind: Pod
metadata:
  name: resources-test
  labels:
    name: roboshop 
    purpose: resources-test
    env: dev
    project: roboshop-dev
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      requests:
        memory: "64Mi"
        cpu: "250m"
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
    - containerPort: 80